defaultBaseImage: localhost:8000/podman-build/base:v1.0.0-7-g1d01a7e

defaultPlatforms:
  - linux/amd64
  - linux/arm64

builds:
  - id: cowsay
    dir: .
    main: ./cmd/cow-say
    ldflags:
      - -s -w
      - -X main.version={{.Env.VERSION}}
